# Dokploy → PipraPay Application → Environment
# DB_HOST = MariaDB service name in Dokploy (e.g. piprapay-mariadb), never localhost.

# Container listen port — must match Dokploy Application → Port (NOT the public domain port).
# Nixpacks (current): PORT=80
# Dockerfile build:  PORT=8080
PORT=80

# Public URL (HTTPS) — fixes Mixed Content for CSS/JS behind Dokploy/Traefik/Cloudflare
PIPRAPAY_APP_URL=https://pay.taqwamart.bd

# Optional: skip web installer after DB is ready (writes pp-config.php on start)
# PIPRAPAY_AUTO_DB_CONFIG=1
# DB_HOST=piprapay-mariadb
# DB_PORT=3306
# DB_DATABASE=piprapaydb
# DB_USERNAME=piprapay
# DB_PASSWORD=CHANGE_ME
# DB_PREFIX=pp_

